# Baseten (https://app.baseten.co) — required only when a service backend = "baseten"
BASETEN_API_KEY=
BASETEN_SAM_MODEL_ID=
BASETEN_STT_MODEL_ID=
BASETEN_FACE_MODEL_ID=

# TypeSafe Jev — required only when services.jev.backend = "typesafe"
TYPESAFE_API_KEY=

# Used ONLY at Truss build time (gated facebook/sam3.1 checkpoint) — never read by the hub
HF_ACCESS_TOKEN=
